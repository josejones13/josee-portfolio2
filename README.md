# Josée Jones — Express EJS Portfolio (Placeholder)

Soft-pink themed portfolio starter built with Express and EJS.
Replace placeholder text and images under `views/` and `public/images/`.

## Run locally

1. Install dependencies:
   ```
   npm install
   ```

2. Start the server:
   ```
   npm start
   ```

3. Visit `http://localhost:3000`

## Notes
- Bootstrap and Font Awesome are included via CDN in `views/layout.ejs`.
- Footer contains placeholder GitHub/LinkedIn icons. Replace the `href="#"` with your profile links.
- This project uses minimal dependencies to keep deployment simple.
